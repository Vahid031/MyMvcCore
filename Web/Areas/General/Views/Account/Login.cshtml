@model ViewModels.General.LoginViewModel
@{
    ViewBag.Title = "ورود به سایت";
}

@*<form method="post" asp-action="Login">
    <div class="row">
        <div class="form-group col-lg-12 col-md-12 col-sm-12 col-xl-12">
            <label class="float-right" asp-for="@Model.UserName"></label>
            <input type="text" asp-for="@Model.UserName" class="form-control" />
            <span asp-validation-for="@Model.UserName"></span>
        </div>
    </div>
    <div class="row">
        <div class="form-group col-lg-12 col-md-12 col-sm-12 col-xl-12">
            <label class="float-right" asp-for="@Model.Password"></label>
            <input type="password" asp-for="@Model.Password" class="form-control" />
            <span asp-validation-for="@Model.Password"></span>
        </div>
    </div>
    <div class="row">
        <div class="form-group col-lg-12 col-md-12 col-sm-12 col-xl-12">
            <input type="submit" value="ورود" class="btn btn-primary" />
        </div>
    </div>
</form>

@section scripts {
    <script>
    </script>
}*@